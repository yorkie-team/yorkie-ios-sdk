version: '3.3'

services:
  yorkie:
    image: 'yorkieteam/yorkie:0.4.13'
    container_name: 'yorkie'
    command: ['server', '--enable-pprof']
    restart: always
    ports:
      - '8080:8080'
      - '8081:8081'
